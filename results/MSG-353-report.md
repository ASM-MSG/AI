# MSG-353 — /highlights 선분석 EC2 실측

> 측정일 2026-08-10 · AI 전용 t3.small (MSG-339 분리) · 원자료 `results/MSG-353-highlights.json`
> 스펙 성공 기준 "성능" 행의 판정 근거. 방법: 인스턴스 로컬 `curl -F` 왕복(업로드 포함), 각 3회.

## 수치

| 케이스 | 중앙값 | 판정 |
|---|---|---|
| 29초 1080p (crowd, 유휴) | **5.28초** | 파이프라인 highlight 단계 실측(2.9~4.7초)과 같은 자릿수. 기준 충족 |
| 4K 30초 (업스케일본, 유휴) | **18.53초** | 예상 상한(직분석 30.9초)보다 낫다. 디코딩 지배 가설 그대로 |
| 174초 1080p (상한 근처, 유휴) | **31.87초** | PRD 기대(3분 원본 20~30초)를 약간 상회. 아래 참고 |
| 29초 1080p, **블러 잡 PROCESSING 중** | **16.16초** | 유휴 대비 약 3배. 큐 대기(최대 3분)는 없지만 CPU 경합은 남는다 |

계약 검증(실영상): crowd 29초 → 구간 3개, 전부 5초 이상, 시작점 간격 전부 5초 이상,
순서는 장면 랭킹. 스펙 D-3 규칙이 실영상에서 그대로 성립한다.

## 읽는 법

- **위저드 기본 시나리오(30초 내외 원본)는 5초대**로, 확정 전 대기로 성립한다.
- **3분 상한 원본 32초**는 PRD가 적은 20~30초를 2초 넘는다. 원본 길이에 정직하게 비례한
  결과라(174초 = 29초의 6배, 시간은 6배) 소프트웨어로 줄일 여지가 거의 없다. FE가 상한
  근처 원본에서 로딩 UI를 길게 잡거나, 상한을 2분대로 내리는 것이 레버다. 판단은 FE·기획 몫.
- **경합 3배(5.3→16.2초)**는 t3.small 2 vCPU를 YOLO가 점유한 대가다. 다만 위저드 선분석
  시점은 업로드 확정 전이라 **자기 블러 잡은 아직 없다**. 겹치는 건 다른 사용자의 잡뿐이고
  dev 트래픽에서는 드물다. 실사용에서 경합이 상시화되면 블러 워커 nice 하향이나 인스턴스
  증설이 다음 레버다.

## 결론

스펙 성공 기준 "성능" 행 충족으로 판정한다. 잔여 리스크 2건(상한 원본 +2초, 경합 3배)은
수치와 레버를 위에 남겼고 현 단계 조치는 불요하다.
